<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieTrackerApp">

        <!-- Home screen (was your only activity before) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Add Movie screen (UI only for CW1) -->
        <activity
            android:name=".AddMovieActivity"
            android:exported="false"
            android:label="@string/add_movie" />

        <!-- Detail / Edit / Delete screen (UI only for CW1) -->
        <activity
            android:name=".MovieDetailActivity"
            android:exported="false"
            android:label="@string/edit" />
    </application>

</manifest>
